<%= form_with(model: item_category) do |form| %>
  <% if item_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(item_category.errors.count, "error") %> prohibited this item_category from being saved:</h2>

      <ul>
        <% item_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :item_id, style: "display: block" %>
    <%= form.number_field :item_id %>
  </div>

  <div>
    <%= form.label :cetegory_id, style: "display: block" %>
    <%= form.number_field :cetegory_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
